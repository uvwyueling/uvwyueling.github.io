{
  "name": "online-portfolio-2026",
  "private": true,
  "description": "源码在 src/，产物 assets/app.js 必须提交（GitHub Pages 直接发布仓库，无 CI 构建）",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --format=iife --outfile=assets/app.js"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
