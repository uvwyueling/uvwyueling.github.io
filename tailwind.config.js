/** Tailwind 配置：只为 echo-interview_page.html 生成 CSS。
 *  版本锁死 3.4.17 —— 与该页原先用的 cdn.tailwindcss.com 所发版本一致，
 *  保证生成结果和原来运行时编译的逐字相同。 */
module.exports = {
  content: ['./echo-interview_page.html'],
};
